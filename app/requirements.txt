pyserial>=3.5
# Verifies the Ed25519 signature on the published firmware list. Imported lazily in
# firmware_manifest.py, so a stale environment costs the version picker rather than
# stopping the app from opening at all.
cryptography>=42.0
